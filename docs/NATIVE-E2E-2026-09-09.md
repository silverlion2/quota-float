# Windows native E2E — 2026-09-09

Validated source `c774e45` using the isolated `app.quotafloat.desktop.e2e` application, synthetic provider data, and Edge/WebView2 152.0.0.0. The test build did not install or update the production application or use real provider credentials.

`npm run test:e2e:build` succeeded, including the frontend test-mode build and optimized native compilation. Tested executable SHA-256: `A0A8F60F560BE0EF0E5AD203EDC780642C944E8504286EC1BFE08EFDFABCC5B6`.

`npm run test:e2e` exited 0 with all six scenarios passing:

- Tauri startup and test bridge availability;
- provider pause/resume persisted through native preferences;
- detached Cockpit window creation and closing using synthetic native cache;
- accessible Control Center opening and closing;
- local weekly/monthly reports with explicit filter scope;
- update dialog opening without navigation.

WDIO session: `7e9411e7-0c5c-455c-99c9-f61a7a415215`. The run completed at approximately 01:56 Asia/Shanghai. Assertions took 5.1 seconds; the runner reported 14 seconds total. Local logs are retained in `output/review-2026-09-09/native-build.log` and `output/review-2026-09-09/native-e2e.log`.

The embedded-driver run emits the same non-fatal external `tauri-driver` diagnostic and teardown mock-store warning recorded in the preceding E2E report. All native assertions and the runner exit code passed; these messages are not reported as failed application tests.

## Limits

This is a functional native smoke test, not a real-provider compatibility run, performance benchmark, full accessibility audit, multi-monitor/DPI acceptance test, or macOS runtime test. Windows installer/launch/upgrade/rollback/uninstall evidence belongs to the separate release workflow. The local E2E executable is not a distribution artifact; public artifacts are rebuilt and signed in release CI. Version-only release metadata changes follow the tested source; the version and exact published commit are recorded in the release report.
